"""Pipeline utilities.

- live_scanner.py — live in-game market scanner (used by `evmax opps`)

Note: pipeline/runner.py is a Phase 1 legacy module and is NOT used in the live scan
pipeline. The live pipeline runs through AgentCoordinator in agents/coordinator.py.
"""
