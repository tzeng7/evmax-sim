"""Venue adapters: fetch and parse golf FIELD markets (one YES contract per
golfer) from Kalshi and Polymarket US into ``FieldPrices``. Parsers are pure
over the venue payload so they test against captured fixtures."""
