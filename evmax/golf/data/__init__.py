"""Golf data adapters: free field/results (ESPN) + free per-category SG (PGA
Tour public GraphQL). Parsers are pure functions over the JSON payload so they
are unit-testable against captured fixtures with no network."""
