# Drift audit — 2026-09-07

| | |
|---|---|
| Scope | `all` |
| Audited SHA | `8b05003` (local `main`); branch cut from `origin/main` `194738b` — the only delta is regenerated state JSON (`data/model_config.json`, `ncaaf_efficiency_state.json`), no doc/code change, so every finding holds at both |
| Branch | `drift-audit/2026-09-07` (isolated worktree via `scripts/sched_worktree.py open`) |
| Findings | 13 — 11 auto-fixed (SAFE, docs only), 2 proposed (RISKY) |

## Deterministic checks

| Check | Result |
|---|---|
| `evmax categories validate` | ✅ PASS — registry valid, 17 categories |
| `pytest tests/ -q` | ✅ PASS — **3555 passed**, 43 warnings, 58s |
| `scripts/check_kalshi_series.py` | ✅ **0 STALE** / 66 OK / 270 NEW (informational — sibling series matching a sector prefix that we deliberately do not poll) |
| `scripts/check_doc_sync.py` | Advisory only. Reports zero-coverage sector-handler modules (`evmax/sectors/*.py`); pre-existing test-coverage debt, not drift — no finding raised |
| Reference scan | ✅ Every `scripts/*.py`, `evmax/…`, `data/models/*.json`, `data/…`, `.claude/…` and `docs/…` path named in `CLAUDE.md` / `README.md` / `TODO.md` / `docs/**` / MEMORY resolves on disk |
| CLI-reference scan | 76 distinct `evmax …` invocations extracted; 1 broken (finding 6). `agents rescan` and `cleanup close-lines` also do not exist but are *correctly* documented as never-built / removed — not findings |

No failing test, so no functionality drift surfaced through the suite.

## Findings

| # | Taxonomy | Class | Doc location | Code / truth location | Disagreement | Action |
|---|---|---|---|---|---|---|
| 1 | Functionality | SAFE | `CLAUDE.md:13` | `data/categories.yaml` (`worldcup.mode`) | Header calls `worldcup` a **shadow** sector; YAML ships `mode: disabled`. The section also instructs `cleanup shadow promote worldcup` "once n≥30 resolved", unreachable while disabled persists nothing | Header now says `disabled`; added the PR #185 reason + the fact that promotion is gated behind re-enabling |
| 2 | Functionality | SAFE | `CLAUDE.md:19` | `data/categories.yaml` | Mode table: "every other category ships `shadow`" — false for `nba_props`, `baseball_props`, `worldcup`, all `disabled` | Sentence corrected (the "read it from `categories modes`" disclaimer kept) |
| 3 | Functionality | SAFE | `CLAUDE.md:232`, `CLAUDE.md:430` | `evmax/sectors/soccer_leagues.py:29` `SOCCER_LEAGUES` | Doc enumerates 8 canonical league keys; code has **13** (adds `ligamx jleague eredivisie brasileirao championship`). The same CLAUDE.md paragraph documents those five being wired 2026-09-05 — internally inconsistent | Both enumerations updated to the 13 keys, anchored on the `SOCCER_LEAGUES` symbol |
| 4 | Functionality | SAFE | `CLAUDE.md:468`, `CLAUDE.md:479` | `data/categories.yaml` (`wnba.disabled_market_types`) | "Spread + total remain shadow" — they are `disabled_market_types`, so the scanner persists neither. CLAUDE.md's own Daily Workflow states this correctly | Reworded: scanner-disabled, shadow rows come from the `watch-listings --log-entries` anchored-entry trigger |
| 5 | Functionality | SAFE | `README.md:1629,1637,1639` | `data/categories.yaml` | Category table lists `worldcup` / `nba_props` / `baseball_props` as `shadow`; all three are `disabled` | Three rows corrected. Also filled in the `nfl` row's missing `spread`+`total` shadow refinement |
| 6 | Reference | SAFE | `README.md:1641` | `evmax/sectors/registry.py` `_REGISTRY` | Claims `valorant` **and `f1`** are registry-latent sectors. `f1` is NOT in `_REGISTRY` — `get_handler("f1")` raises `KeyError`; only the handler *file* exists. `CLAUDE.md:7` states this correctly | README brought in line with CLAUDE.md and the code |
| 7 | Reference | SAFE | `README.md:1560-1561` | `evmax/cli/commands/report.py:26,49` | `evmax report bankroll` → `No such command 'bankroll'`; bare `evmax report` → `Missing command`. Bankroll history is a **flag** on a `report report` subcommand | Corrected to `evmax report report` / `evmax report report --bankroll` |
| 8 | Ownership | SAFE | `docs/SCHEDULED_RUNS.md:105-113` | `scripts/sched_worktree.py` (merged `a4ebb87`, PR #259) + all 8 task `SKILL.md`s | "Rollout status" still reads as pending ("Tasks to convert: …"). The helper is merged and **all 7 listed tasks + the model-improve graph call it today** (verified by grepping each `SKILL.md`) | Marked COMPLETE, with the rule that new state/code tasks start on this shape |
| 9 | Ownership | SAFE | `docs/SCHEDULED_RUNS.md:159,160,162` | `list_scheduled_tasks` + this file's own 2026-09-05 History entry | `daily-morning-scan`, `daily-updated-scan`, `weekly-nba-props-shadow-metrics` listed as merely "Disabled"; the file's own History says the four zombie tasks were **deleted** 2026-09-05, and none is registered | Rows re-labelled DELETED; `baseball-spread-lay-gate-check` row added for the fourth |
| 10 | Functionality | SAFE | `docs/SCHEDULED_RUNS.md:161` | Same file, Daily table | "no automated scan currently runs" — contradicted by the three enabled `ev-scan-light-*` tasks (plus three NFL waves) documented 30 lines above | Corrected to point at the 2026-07-19 handover |
| 11 | Ownership | SAFE | `docs/SCHEDULED_RUNS.md:233-235` | `00b060d` (PR #177), `evmax/cli/commands/cleanup.py` | `prune-stale` "becomes functional once the feature branch `claude/stale-positions-cleanup-bbec57` merges to `main`" — merged long ago; the command is live and the task has been firing every 2h | Note replaced with the merge ref |
| 12 | Ownership | SAFE | `docs/SCHEDULED_RUNS.md` (absent) | `list_scheduled_tasks` | Three **enabled** tasks were undocumented: `ev-scan-nfl-sunday-late` (Sun 12:45), `weekly-nfl-props-shadow-metrics` (Mon 08:30), `nfl-week1-seed-verify-2026-09-14` (one-time) | Two rows added to Daily/Weekly; a new "One-time" section added for the third |
| R1 | Functionality | **RISKY** | `data/categories.yaml` (`nba_props.notes`) | Same file, `nba_props.mode` | The `notes:` string says "Demoted to shadow 2026-05-01" while the entry's own `mode:` is `disabled` (PR #185). The note is stale prose *inside* a data file | **Not applied.** Any edit to `data/categories.yaml` is RISKY by rule, even a `notes:` string. Recommended: append the PR #185 disabling to `nba_props.notes` (and check `baseball_props` / `worldcup` notes for the same lag) |
| R2 | Ownership | **RISKY** | `.claude/commands/drift-audit.md` Phase 3 + `~/.claude/scheduled-tasks/weekly-drift-audit/SKILL.md` | `docs/SCHEDULED_RUNS.md:80-85,110-112` | Two docs prescribe **different git procedures for this same task**. The audit spec says `git switch -c drift-audit/<date>` in the SHARED checkout and `git switch main` at the end; SCHEDULED_RUNS says code tasks open an isolated worktree via `sched_worktree.py` and that the shared checkout "stays permanently on a clean `main` … no task ever runs `git switch`". The SKILL has *already* adopted `sched_worktree.py watch-ci` for the CI gate, so the conversion is half-done | **Not applied** — it changes how a future run behaves, and half of it (the SKILL.md) lives outside the repo, so a repo-only fix would deepen the inconsistency. **This run followed the stricter SCHEDULED_RUNS policy** (isolated worktree; shared checkout never left `main`). Recommended: finish the conversion — rewrite Phase 3 of `.claude/commands/drift-audit.md` around `sched_worktree.py open/ship` and update the SKILL.md via the `scheduled-tasks` MCP tools in the same change |

## Verified clean (no drift)

Checked against the named symbol and found to agree exactly:

- **`SECTOR_WEIGHT_OVERRIDES`** — all 12 sector entries (nba, wnba, soccer, worldcup, tennis, baseball, nfl, ncaaf, ufc, nhl, ncaab, ncaaw) match the CLAUDE.md per-sector override list weight for weight.
- **Blend gates** — `REQUIRED_BLEND_MODELS` (tennis, 4 primaries), `MIN_NONSHARP_MODELS` (soccer ML / worldcup ML+advance / ncaaf ML, `min_count: 1`), `_NON_MODEL_TOKENS` (all 10 tokens), ensemble confidence gate `0.45`.
- **`PoissonModelAgent`** — `SUPPORTED_SECTORS` and `SOCCER_LIKE_SECTORS` are both `{"soccer", "worldcup"}`.
- **Elo** — `STALE_DAYS=60`; `K_FACTORS` nhl 6.0 / ncaaw 35 / worldcup 40; `HOME_ADVANTAGE_ELO` nhl 48 / ncaaw 80 / soccer 60 / worldcup 0; NFL absent from both `REST_ELO_ADJ` and `EARLY_K_BOOST`, as documented.
- **Form** — `STALE_DAYS=60`, `MARGIN_FORM_SECTORS` ships empty (the rejected NFL margin-form result).
- **Efficiency stacks** — WNBA 2.6/12.5/MIN_GAMES 4/SHRINK_K 8/clip [65,100]; NBA 3.2/12.0; NCAAB & NCAAW 10.5/6/4 with clips [55,90] and [55,92]; NCAAF 16.5/70/RAMP_K 3/HOME 2.5 and v2 35/70/3.5 + `FPI_PRIOR_SHARE 0.5` + `state_filename` declared + `schema_version: 2` refusal; NFL 2.0/13.5/64/MIN_GAMES 6; NFL QB Elo K 25/HOME 48/share 0.60/clamp 200; NHL xG 2.05/0.18.
- **Sizing & matching** — Kelly cap 5%, per-event exposure 8%, `joint_kelly_enabled=False` + `joint_kelly_max_gross_pct=0.15`, `fuzzy_threshold=88`, spread/total dedup key includes the line (`engine.py:269-270`).
- **Venues & fees** — Kalshi `AsyncLimiter(10,1.0)`, PolyUS `AsyncLimiter(15,1.0)`, Kalshi taker 0.07 / PolyUS taker 0.06 / maker −0.0125, `polymarket_us_live=False` with `polymarket_us_live_sectors="wnba"`.
- **Injury** — `SECTOR_INJURY_URLS` = {nba, ncaab, ncaaw, nfl, soccer, wnba}; `MAX_ADJ=0.20`, per-team `_adj_cap=0.10`; `NFL_POSITION_WEIGHTS` QB 1.5 vs G 0.15 (the documented ~10×).
- **Season windows** — exactly `nfl`, `ncaab`, `ncaaw` have a `live` base *and* a window; `nba`/`soccer`/`tennis`/`wnba` have none. Matches the off-season-downgrade claim verbatim.
- **Alignment & sector sets** — `evmax/matching/alignment.py` (`align_yes_side`, `YesOutcome`, `alignment_looks_suspect`), `PRICE_FALLBACK_SECTORS={lol,cs2}`, `_EVENT_TITLE_SECTORS={tennis,soccer}`, `_SERIES_TEAM_CODE_MAPS={KXMLSGAME,KXUCLGAME}`, `_US_SECTORS` includes `ncaaf` + `ufc`, `ESPN_SPORT_MAP` deliberately omits `ufc`.
- **Weekly-sector persistence** — `persist_window` wired at both `cli/commands/agents.py:399` and `web/app.py:528`; `scan_horizon_days: 7` on `nfl` and `ncaaf` only.
- **launchd** — `com.evmax.integrity` installed and loaded, plist `StartCalendarInterval` is 06:45 + 20:30 exactly as documented; `watch-closes` and `watch-listings` loaded.

## Informational (no action)

- `check_kalshi_series.py` reports **270 NEW** Kalshi series matching a sector prefix (e.g. `KXCS2MAP`, `KXLIGUE1BTTS`, `KXCS2TOTALMAPS`). These are exotic/derivative products we deliberately do not wire; listed here only so a future audit does not re-investigate.
- `com.evmax.discord-bot` is documented as "template only, NOT installed by default" — accurate as a statement about the repo. On this machine it *is* installed and loaded (`launchctl` pid 35156, last exit `-15`). Local state, not doc drift. An untracked `com.evmax.discord-bot.plist` also sits at the repo root.
- MEMORY (`MEMORY.md` → `project_nba_props_shadow.md`) still describes `nba_props` as "demoted to shadow", superseded by the PR #185 flip to `disabled`. Memory lives outside the repo, so it cannot be fixed by this PR.
- `evmax/sectors/*.py` handlers have zero direct test coverage per `check_doc_sync.py`. Pre-existing coverage debt against the CLAUDE.md testing policy, not doc↔code drift.

## Bottom line

**13 findings — 11 auto-fixed, 2 proposed, 8 clean areas.** Every fix is documentation-only; no
`evmax/**/*.py`, `data/categories.yaml`, test, fixture, model-state file or DB was touched. The
deterministic gates were green before and after.
