# Drift audit — 2026-09-14

| | |
|---|---|
| Scope | `all` |
| Audited SHA | `94fc8c1` (`origin/main` tip) |
| Branch | `drift-audit/2026-09-14` (isolated worktree via `scripts/sched_worktree.py open`) |
| Findings | 11 — 9 auto-fixed (SAFE, docs only), 2 proposed (RISKY) |

Prior audit: `docs/drift-audits/2026-09-07.md`. Its clean-area list still holds for every
symbol whose module did not change in the interval, so this run concentrated on the 20 source
files touched by PRs #271–#286 (NFL weekly reseed, playlist shadow-stake fix, NFL SR-in-margin,
tennis challenger probe, NFL preseason-prior ramp, alt-spread ladder, venue expansion + devig
selection) plus a full reference/CLI/scheduled-task re-scan.

## Deterministic checks

| Check | Result |
|---|---|
| `evmax categories validate` | ✅ PASS — registry valid, 17 categories |
| `pytest tests/ -q` | ❌ **1 failed, 3632 passed**, 43 warnings, 78s — `tests/test_integrity.py::TestRunIntegrity::test_one_alert_at_worst_severity_sorted_worst_first`. Captured as **R1**; not "fixed" (guardrail: never edit a test to make a check pass) |
| `scripts/check_kalshi_series.py` | ✅ **0 STALE** / 66 OK / 271 NEW (informational — sibling series matching a sector prefix that we deliberately do not poll) |
| `scripts/check_doc_sync.py` | Advisory only; no finding raised |
| Reference scan | 43 unresolved paths across `CLAUDE.md` / `README.md` / `TODO.md` / `docs/**` / MEMORY; triaged below — 4 are real findings, the rest are historical records, forward-looking `new …` paths, runtime-created state, or illustrative tutorial filenames |
| CLI-reference scan | 65 distinct `evmax …` invocations resolved against the live Typer app. 3 non-prose misses: `agents rescan` and `cleanup close-lines` are *correctly* documented as never-built / removed; `evmax report bankroll` survives only inside the 2026-09-07 audit record, which is history |
| Scheduled-task cross-check | ✅ All 19 registered tasks (18 enabled + `weekly-value-audit` paused) resolve to a `docs/SCHEDULED_RUNS.md` row at the matching cadence; every documented job has a backing task. No ownership drift |

## Findings

| # | Taxonomy | Class | Doc location | Code / truth location | Disagreement | Action |
|---|---|---|---|---|---|---|
| 1 | Functionality | SAFE | `CLAUDE.md:402-403` | `evmax/agents/cleanup/integrity.py:662` | `--weekly` documented as adding "calibration bias + promotion-gate clearances". `WEEKLY_CHECKS = ("calibration", "gates", "devig")` — the third, `check_devig` (PR #280), is undocumented here even though CLAUDE.md's own `ev/devig.py` tree entry says the weekly sweep is what surfaces the recommendation | Enumeration completed, anchored on the `WEEKLY_CHECKS` symbol |
| 2 | Functionality | SAFE | `docs/SCHEDULED_RUNS.md` (`com.evmax.integrity` row) | same symbol | Same omission in the launchd table | Row updated with the symbol + the one-tap `cleanup devig promote` it emits |
| 3 | Functionality | SAFE | `CLAUDE.md` (sharp-only guard paragraph) | `evmax/agents/odds/ev_gap_agent.py:258-265` | `_NON_MODEL_TOKENS` documented as 10 tokens; PR #278 added an 11th, `sharp_ladder`. A ladder-priced row is the sharp price, so it must not count toward `MIN_NONSHARP_MODELS` — the doc enumeration would mislead anyone reasoning about the soccer/worldcup/ncaaf guard | `sharp_ladder` added to the documented list |
| 4 | Functionality | SAFE | `README.md:669` | `evmax/agents/models/elo_agent.py:83,156` | "generic Elo is held at 0 for NHL because its K-factor / home-advantage **have never been calibrated for hockey**". False since MODEL-2's NHL half closed 2026-07-18: `K_FACTORS["nhl"]=6.0`, `HOME_ADVANTAGE_ELO["nhl"]=48.0`, and `elo_state.json['nhl']` is seeded (32 teams, 246 games each). `CLAUDE.md` and `TODO.md:737,870` already state the corrected version — README was the lone outlier | Reason replaced: the 0 is an unmade **blend decision**, not a blocked calibration; raising it needs a walk-forward |
| 5 | Functionality | SAFE | `README.md:7` | `evmax/settings.py:48,60` + `venue_sector_live` | "Polymarket US is behind a venue shadow firewall (`polymarket_us_live=false`): its gaps log as `mode='shadow'` … until the venue clears the gates". The firewall has been **per-sector** since 2026-08-22 and ships `polymarket_us_live_sectors="wnba"`, so WNBA moneyline on PolyUS is live today. README describes a blanket block that no longer exists | Rewritten around `venue_sector_live` + the allowlist, incl. the "category mode still gates" rule and why only WNBA *moneyline* clears |
| 6 | Functionality | SAFE | `README.md:1686` | same | "All Polymarket US gaps log as shadow until the venue firewall lifts" — same blanket claim in the coverage note | Corrected to the allowlist semantics |
| 7 | Reference | SAFE | `README.md:1586` (env table) | `evmax/settings.py:60` | `POLYMARKET_US_LIVE_SECTORS` (shipped default `wnba`) is absent from the env table that documents its master switch | Row added next to `POLYMARKET_US_LIVE`, which is relabelled MASTER switch |
| 8 | Ownership | SAFE | `TODO.md:455-460` (ARCH-1) | `0f27511` (2026-06-19) | ARCH-1 prescribes deleting `evmax/pipeline/runner.py`, `evmax/models_ml/sharp_only.py` and `evmax/clients/pinnacle.py`. **All three were deleted 3 months ago** in that commit (the whole `evmax/pipeline/` package is gone). Open work that is already done | Marked ✅ DONE with the commit ref; item retained because ARCH-9 reads as its follow-on |
| 9 | Reference | SAFE | `TODO.md:652-654` (ARCH-9) | `0f27511`; `evmax/cli/commands/agents.py` | ARCH-9 asserts in the present tense that `evmax/clients/pinnacle.py` "is a fully-implemented TheOddsAPI wrapper (~900 lines) … never deleted", "currently dead code", "imported … by one vestigial `get_quota()` display call in `evmax/cli/commands/agents.py:382`". The file is gone, the importers are gone, and `get_quota`/`_quota` have **zero** occurrences in `agents.py`. The item's whole cost estimate rests on a client that no longer exists | Premise block rewritten: this is now *re-implement*, not *resurrect*; recovery path `git show 0f27511^:…` given so the effort can be re-priced |
| 10 | Reference | SAFE | `TODO.md:230` (MODEL-7) and `TODO.md:421` (MODEL-16) | `edb3d7b` (2026-05-10) | Two **forward-looking** `Files:` headers name files that no longer exist: `evmax/backtest/sources/nfl_props.py` and `scripts/calibrate_nba_props.py`. Neither task can be executed as written. (The `✅`-marked "Recently Shipped" log entries naming the same paths are historical records and were left alone) | Both headers annotated with the deletion + a `git show` recovery path |
| R1 | Functionality | **RISKY** | `tests/test_integrity.py:220-224` | `evmax/agents/cleanup/integrity.py:601,662` | `_mock_all_checks` stubs 13 runners but **not `check_devig`**, which PR #280 added to `WEEKLY_CHECKS`. So `test_one_alert_at_worst_severity_sorted_worst_first` (the only `weekly=True` + assert-exact-issues test) calls the **real** `check_devig`, which reads the live `predictions.db`/`archive.db`. It returns a 4th `info` issue on this machine and the test fails. This is a **non-hermetic test**, not a bad assertion: it passes in CI (no local DB) and fails on the operator's box, which is exactly backwards. CLAUDE.md's Testing Policy makes a matching `tests/` change mandatory for `evmax/agents/cleanup/` edits — the mock helper was the missed half | **Not applied.** Fixing it means editing a test, which the guardrail forbids this run from doing to turn a check green. Recommended one-line fix: add `"check_devig"` to the `names` set at `tests/test_integrity.py:220-224`. Worth also asserting `set(ig.WEEKLY_CHECKS) \| set(ig.DAILY_CHECKS)` is fully covered by the helper, so the next added check fails loudly at the mock rather than silently reaching real data |
| R2 | Functionality | **RISKY** | `data/categories.yaml:157` (`nhl.notes`) | `evmax/agents/models/elo_agent.py:83,156` | "Elo K-factor still uncalibrated for NHL (see MODEL-2 / SECTOR-1) — kept out of NHL ensemble for now". Same stale claim as finding 4, but inside the registry data file | **Not applied** — any edit to `data/categories.yaml` is RISKY by rule, even a `notes:` string. Recommended: fold into one pass with the still-open 2026-09-07 **R1** (`nba_props.notes` says "Demoted to shadow" while its `mode:` is `disabled`); both are prose lagging inside the same file |

### Carried over from 2026-09-07 (still open)

- **R1 (2026-09-07)** — `nba_props.notes` vs `nba_props.mode`. Unchanged. Pair it with R2 above.
- **R2 (2026-09-07)** — `.claude/commands/drift-audit.md` Phase 3 (`git switch -c` in the shared
  checkout) still disagrees with `docs/SCHEDULED_RUNS.md` (isolated worktree via
  `sched_worktree.py`). Unchanged; `weekly-drift-audit` remains ❌ in the rollout table. **This run
  again followed the stricter SCHEDULED_RUNS policy** — isolated worktree, shared checkout never
  left `main` — and it worked end to end for the second consecutive week.

## Verified clean (no drift)

Re-checked against the named symbol in the interval's changed modules:

- **Alt-spread ladder (PR #278)** — `SPREAD_LADDER_ENABLED = False`, `SPREAD_MAX_SIGMA = 1.0`,
  `_SPREAD_MAX_ABS_LINE = {}` (all inert, exactly as documented); client emits
  `::spread::<line>` for `isAlternate` rungs (`esports_pinnacle.py:1011`), `SharpOdds.is_alternate`
  exists, `engine._find_nearest_spread` at `engine.py:246`, `sharp_ladder` token in `ev_gap_agent`,
  golden fixtures at `tests/test_spread_alt_ladder.py`.
- **NFL efficiency prototypes (PRs #273, #276)** — `EPA_MARGIN_PTS = PLAYS_PER_TEAM_GAME` (64),
  `SR_MARGIN_PTS = 0.0`, `PRESEASON_PRIOR_ENABLED = False`, `PRESEASON_PRIOR_CONFIDENCE = 0.45`,
  `HOME_EDGE_PTS 2.0 / SCORE_STDEV 13.5 / MIN_GAMES 6`. Both prototypes genuinely inert.
- **Devig selection (PR #280)** — gate constants `MIN_N 200` / `MIN_BRIER_DELTA 0.002` / `MIN_Z 1.64`
  match the documented "Δ≥2/1000 AND z≥1.64 AND n≥200"; `DEVIG_METHOD_BY_SECTOR = {}`;
  precedence in `resolve_devig_method` is code-override → state JSON → `settings.devig_method`;
  `evmax cleanup devig show/promote/clear` all exist.
- **Venue expansion (PR #280)** — `MarketSource.novig`/`.prophetx` + `VENUE_LABELS` entries;
  `novig_enabled`/`prophetx_enabled` False; `NOVIG_TAKER_RATE 0.03`, `NOVIG_MAKER_RATE_MULT −0.5`,
  `PROPHETX_COMMISSION_RATE 0.02`. The documented drag at p=0.5 reproduces: Kalshi
  `0.07·0.25 = 1.75pp`, ProphetX `1/(1+0.98) − 0.5 = 0.505pp`.
- **Generic venue firewall (PRs #272, #280)** — all three enforcement sites now route through
  `settings.venue_sector_live(venue, sector)`: `coordinator._venue_is_live`, `logger.log_gaps`,
  `web/playlist.gap_to_dict` (`web/app._gap_to_dict` survives as an alias, so the CLAUDE.md
  reference still resolves).
- **Anchored entry** — `watch-listings --entry-sectors` default is `"wnba,nfl"` as documented.
- **`POLYMARKET_US_LEAGUE_MAP`** — 8 sectors, matching both docs.
- Unchanged modules (`ensemble_agent.py`, `poisson_agent.py`, `elo_agent.py`, `form_agent.py`,
  the efficiency stacks, `alignment.py`, `categories.py`) were not re-derived — the 2026-09-07
  clean-area list stands, and `git diff 1bd86d9..HEAD` confirms none of them changed.

## Informational (no action)

- **`check_devig` is reporting a live, actionable recommendation**, surfaced incidentally by R1:
  `ncaaf: multiplicative beats power by 4.2/1000 Brier (z=15.3, n=6563)` — clears all three gates.
  Applying it is `evmax cleanup devig promote ncaaf`. This is an **operational decision, not drift**,
  so this run neither applied nor recommended it; the weekly integrity sweep is its proper owner.
- 39 of the 43 unresolved doc paths are not drift: memory files live outside the repo;
  `docs/drift-audits/*.md` deliberately record deleted things; TODO's `**Files:** new …` headers are
  design targets; `data/seeds/*.json` in README are illustrative tutorial filenames whose JSON shape
  is shown inline; `data/models/devig_method_state.json` and `data/models/.nba_api_breaker.json` are
  runtime-created on first write.
- `com.evmax.discord-bot.plist` still sits untracked at the repo root (noted 2026-09-07); six
  untracked `scripts/exp_wnba_*.py` / `scripts/exp_ladder_arb_scan.py` experiment scripts and
  `data/backtest/wnba_totals_walkforward_games.json` are likewise uncommitted local work. Left alone
  — not this audit's scope.

## Bottom line

**11 findings — 9 auto-fixed, 2 proposed, 6 clean areas.** Every fix is documentation-only; no
`evmax/**/*.py`, `data/categories.yaml`, test, fixture, model-state file or DB was touched.
`categories validate` is green; the suite is **1 failed / 3632 passed**, and that one failure is
finding R1 itself — reported, deliberately not patched.
