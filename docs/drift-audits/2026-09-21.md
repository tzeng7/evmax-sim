# Drift audit — 2026-09-21

| | |
|---|---|
| **Scope** | all |
| **Audited SHA** | `d757cc8` (origin/main at run time) |
| **Branch** | `drift-audit/2026-09-21` (isolated worktree, `scripts/sched_worktree.py open`) |
| **Runner** | `weekly-drift-audit` scheduled task, spec `.claude/commands/drift-audit.md` |

## Deterministic checks

| Check | Result |
|---|---|
| `evmax categories validate` | ✅ Registry valid, 17 categories |
| `scripts/check_kalshi_series.py` | ✅ OK 66 · **STALE 0** · NEW 291 (informational: unwired sub-series like `KXCS2MAP`, `KXLIGUE11H` — period/prop markets we deliberately don't poll) |
| `pytest tests/ -q` | ✅ 3794 passed (baseline), 3794 passed (post-fix, in worktree) |
| `scripts/check_doc_sync.py` | Advisory only — zero-coverage hints for `evmax/sectors/*`; used to aim the sweep, not a finding |

## Scheduled-task / ownership cross-check

All **20** registered tasks were compared against `docs/SCHEDULED_RUNS.md`. **No ownership drift.**
Every enabled task is documented at its real cadence; every documented job has a backing task;
the disabled/deleted entries (`weekly-value-audit`, `nfl-week1-seed-verify-2026-09-14`, the four
2026-09-05 zombie deletions) match the registry. The `weekly-tennis-rankings-refresh` candidate
named in the task prompt is **correctly documented as deleted** — it is absent from the live
registry and `docs/SCHEDULED_RUNS.md` already records "Task DELETED". No change needed.

## Reference scan

Every `scripts/*.py`, `evmax/...`, and `data/*.json` path named in `CLAUDE.md`, `README.md`,
`TODO.md`, `docs/` and the memory files was resolved against the tree. All 33 documented
`evmax <group> <cmd>` CLI invocations (including all 9 `cleanup shadow` subcommands) exist.
Unresolved paths were triaged and **dismissed as non-findings**:

- `TODO.md` forward references (`evmax/clients/novig.py`, `ps3838.py`, `evmax/pipeline/runner.py`, …) — unbuilt work, correct for a plan doc.
- `README.md` `data/seeds/*.json` — illustrative `--file` argument examples the user supplies, not shipped files.
- `data/models/devig_method_state.json` (`CLAUDE.md:101`) — create-on-demand; written only by `evmax cleanup devig promote`, and no sector has been promoted. `evmax/ev/devig.py:37` reads it defensively.
- `docs/drift-audits/*` and `memory/*` references to removed scripts — immutable historical records.

## Findings

| # | Taxonomy | Class | Doc location | Code location | Disagreement | Action |
|---|---|---|---|---|---|---|
| 1 | Functionality | SAFE | `CLAUDE.md:170` (Modeling table) | `evmax/agents/models/pitcher_agent.py:288`, `evmax/agents/models/base.py:81` | Doc says the MLB pitcher state file is `data/models/pitcher_state.json`. The agent sets `name = "pitcher_v2"` and declares **no** `state_filename`, so `base.py:81` derives `data/models/pitcher_v2_state.json` — which is what the weekly reseed writes (132 KB, mtime 2026-09-21). The v1 `pitcher_state.json` is an orphan (32 KB, mtime 2026-07-18), read by no production path. | Doc corrected; added a note to the "state files are declared, not derived" bullet that letting the file follow the name is the *other* valid resolution, so a future reader doesn't "fix" it back |
| 2 | Functionality | SAFE | `CLAUDE.md:156`, `CLAUDE.md:157` | `evmax/agents/models/shot_quality_agent.py:71`, `evmax/agents/models/matchup_agent.py:57` | Modeling table lists NBA Shot Quality **0.20** and NBA Matchup **0.20**. Code class weights are **0.15** and **0.10**; the NBA `SECTOR_WEIGHT_OVERRIDES` entry is 0.10/0.10. 0.20 matches neither value under either convention, and CLAUDE.md's own per-sector override line already says 0.10/0.10 — the table contradicted the rest of CLAUDE.md as well as the code. Both weights have been 0.15/0.10 since the agents were introduced (2026-04-19), so this was doc drift from day one, **not** a code regression. | Table corrected to the class weights (0.15 / 0.10), matching the column's convention elsewhere (NBA Efficiency 0.40, Possession Sim 0.35 are class weights too) |
| 3 | Functionality + Reference | SAFE | `evmax/agents/models/__init__.py:7-16` (module docstring) | `tennis_model_agent.py:220`, `tennis_serve_return_agent.py:123`, `pitcher_agent.py:289` | The "Model weights and state files" list was stale on three rows: `TennisModelAgent weight=0.45` (actual **0.35** — the agent's own comment even reads "trimmed from 0.45"), `TennisServeReturnAgent weight=0.40` (actual **0.15**), `PitcherModelAgent weight=0.20` + `pitcher_state.json` (actual **0.50** + `pitcher_v2_state.json`). It also omitted `TennisFormAgent`, which the module does export. | Three rows corrected, `TennisFormAgent` added, and a caveat added that these are BASE class weights which `SECTOR_WEIGHT_OVERRIDES` supersedes per sector |
| 4 | Functionality | SAFE | `evmax/agents/models/ensemble_agent.py` (NHL override comment) | `evmax/agents/models/elo_agent.py:83`, `elo_agent.py:156` | Comment claimed generic Elo is held at 0 for NHL "because its K-factor / home-advantage have **never** been calibrated for NHL (MODEL-2 / SECTOR-1)". The NHL half of MODEL-2 closed 2026-07-18: `K_FACTORS["nhl"] = 6.0` and `HOME_ADVANTAGE_ELO["nhl"] = 48.0` both ship, `scripts/backtest_nhl_elo.py` exists, and `elo_state.json['nhl']` is seeded (34 ratings). `CLAUDE.md` already records the closure — only the code comment lagged. | Comment rewritten: the 0 is an **unmade blend decision** needing a walk-forward, not a blocked calibration |
| 5 | Reference | SAFE | `README.md:756`, `scripts/seed_pitcher_fip.py:223`, `evmax/agents/cleanup/heartbeat.py:59` | same as #1 | Three more stale `pitcher_state.json` filename references (a README state-file line, one stderr string, one explanatory comment). The heartbeat comment's *rationale* is still correct — `pitcher_v2_state.json` genuinely has no top-level `fetched_at`/`last_updated` (keys: `pitchers`, `team_starters`, `league_avg_era`, `relievers`, `league_pen_cfip`, `pen_seeded_at`). | All three renamed; the heartbeat comment now enumerates the actual keys so the rationale is verifiable in place |
| 6 | Functionality | SAFE | `evmax/agents/cleanup/heartbeat.py:43` | `heartbeat.py:56`, `data/models/wnba_efficiency_state.json` | Header comment said "The NFL / WNBA / NCAAF / **pitcher** states nest a per-sector `fetched_at`". Two errors: pitcher is not in `_SEED_STATE_CHECKS` at all (see #5), and WNBA's stamp is **top-level** `fetched_at` (`stamp_path: ["fetched_at"]` at `:56`; verified the shipped state's top-level `fetched_at` = `2026-09-21`), not nested. | Comment corrected to "NFL / NCAAF nest a per-sector `fetched_at`, WNBA carries one at the top level" |

## SAFE fixes applied

All six findings were applied on `drift-audit/2026-09-21`. Files touched: `CLAUDE.md`,
`README.md`, `evmax/agents/models/__init__.py`, `evmax/agents/models/ensemble_agent.py`,
`evmax/agents/cleanup/heartbeat.py`, `scripts/seed_pitcher_fip.py`.

**Behaviour neutrality.** The diff is comments, docstrings, markdown and one stderr string.
The `change-validator` gate proved it by AST comparison: `heartbeat.py` and `ensemble_agent.py`
are AST-identical to HEAD, and `__init__.py` / `seed_pitcher_fip.py` differ only in string-literal
contents. `_SEED_STATE_CHECKS` and `SECTOR_WEIGHT_OVERRIDES["nhl"]` are untouched. No test,
fixture, model state file or `data/categories.yaml` was modified. `pytest tests/ -q` → 3794 passed.

**Gate history (worth recording — the audit caught itself twice).** The validator **REJECTED**
the first submission: the rewritten `__init__.py` docstring listed `tennis_form` among "agents not
exported from this package" while `TennisFormAgent` is imported and in `__all__` — a drift audit
introducing drift. It also surfaced findings #5 (README) and #6 (heartbeat header) as misses.
A self-check during the #6 fix then caught a second self-inflicted error: an interim wording
claimed "every state here has a NO-OP `update()`", but `UFCRatingAgent.update()` and
`TennisModelAgent.update()` do real work — reverted to the original's narrower scoping.
Re-validation returned **ACCEPT** with all 13 factual claims independently verified against source.

## RISKY — needs your decision

**None.** No finding this week required a code-behaviour change, and no case arose where the code
looked like the side that had drifted.

Two follow-ups were raised by the validator and deliberately **not** acted on, because each would
be a behaviour change and therefore outside this audit's mandate:

1. **`pitcher_v2_state.json` has a latent freshness stamp.** `pen_seeded_at` is a real date
   (`'2026-09-21'` in the shipped state), so the file *could* be age-checked by adding
   `{"file": "pitcher_v2_state.json", "sector": "baseball", "stamp_path": ["pen_seeded_at"], ...}`
   to `_SEED_STATE_CHECKS` (`evmax/agents/cleanup/heartbeat.py:48`). Today a silently failing MLB
   pitcher reseed is caught by nothing. Note `pen_seeded_at` stamps only the *bullpen* block, so it
   is a partial proxy for whole-file freshness — worth a deliberate decision, not a drift fix.
2. **`tennis_form` has no row in the CLAUDE.md Modeling table** despite carrying the largest tennis
   override weight (0.35, `ensemble_agent.py`). Every other tennis model has a row. Adding one is a
   doc *addition* rather than a drift correction, so it is left for a scoped follow-up.

## Bottom line

**6 findings — 6 auto-fixed, 0 proposed, 5 clean areas** (categories registry · Kalshi series map ·
scheduled-task ownership · CLI/reference surface · per-sector ensemble weights, gates and model
constants). The semantic sweep re-verified and found **in agreement**: `SECTOR_WEIGHT_OVERRIDES`
for all 11 sectors, `REQUIRED_BLEND_MODELS`, `MIN_NONSHARP_MODELS`, `_NON_MODEL_TOKENS`,
`PoissonModelAgent.SUPPORTED_SECTORS`/`SOCCER_LIKE_SECTORS`, Elo/Form `STALE_DAYS`, the NBA /
WNBA / NCAAB / NCAAW / NCAAF / NFL / NHL model constants, `SOCCER_LEAGUES` (13 keys),
`POLYMARKET_US_LEAGUE_MAP` (8 sectors), `_US_SECTORS`, `PRICE_FALLBACK_SECTORS`, the venue fee
models, the devig selection gate, the Kelly/exposure caps, `WEEKLY_CHECKS`, `persist_window`,
the spread-ladder flags, and every category mode/resolver/market-type field.
