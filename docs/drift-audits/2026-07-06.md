# Drift audit — 2026-07-06

- **Scope:** all (weekly automated run)
- **Audited SHA:** `8d2cb32` (origin/main, post PR #74 — World Cup advance markets)
- **Branch:** `drift-audit/2026-07-06`
- **Deterministic checks:**
  - `evmax categories validate` → ✓ **15 categories**
  - `pytest tests/ -q` → **2011 passed, 5 failed** — all 5 failures are in the **untracked** file `tests/test_wnba_efficiency_carryover.py` (imports the untracked `scripts/experiment_wnba_ml_carryover.py` + `seed_wnba_efficiency` helpers). No committed-code test failed. (Prior audit on committed main `d68cd36`: 1944 passed / 7 skipped.)
  - `check_doc_sync.py` → advisory zero-coverage nudges only, no drift.

## Findings

| # | Taxonomy | SAFE/RISKY | Doc side | Code / reality side | Disagreement | Action |
|---|---|---|---|---|---|---|
| 1 | reference / functionality | **SAFE** | `CLAUDE.md:7` "Current catalog (14 categories)"; `README.md:1535` "14 categories ship today" + catalog table | `data/categories.yaml` (15 keys); `evmax categories validate` → 15; `baseball_props` added 2026-06-27 (shadow/wip) | Both docs say **14** and omit `baseball_props`; the README table had no row for it | **Fixed** (`2f8dbd4`) — count → 15, `baseball_props` added to the CLAUDE.md props list and a new README table row |
| 2 | ownership | **SAFE** | `docs/SCHEDULED_RUNS.md` Daily table lists `daily-morning-scan` (01:01) + `daily-updated-scan` (09:05) as active | `list_scheduled_tasks`: both `enabled:false` since 2026-07-02 (lastRun 07-02). `ev-scan-90min-on-hour` + `ev-scan-90min-half-hour` are `enabled:true` and **undocumented** | Disabled scans shown as active; the replacement rolling ~90-min scan+push-notify pair not documented anywhere | **Fixed** (`af63a76`) — moved the two fixed scans to the Disabled table, documented the `ev-scan-90min-*` pair in the Daily table, added a 2026-07-02 History note |
| 3 | test signal | **RISKY / observation** | — | `tests/test_wnba_efficiency_carryover.py` (untracked) → 5 failures | Failures come from the user's **uncommitted WIP** (WNBA prior-season carryover experiment), not committed-code drift | **Not acted on** — out of scope, and a guardrail forbids editing tests to make checks pass. Reported only. Resolve or discard the WIP before it lands. |
| 4 | ownership | **RISKY (carryover)** | CLAUDE.md tennis section says `weekly-tennis-rankings-refresh` was "deleted" | Task exists with `enabled:false` (lastRun 2026-06-23) | Disabled ≠ deleted; the task folder is still present with a stale Sackmann-era prompt | Carryover from the 2026-07-01 audit. `SCHEDULED_RUNS.md` already flags it safe-to-delete. **Recommend** deleting `~/.claude/scheduled-tasks/weekly-tennis-rankings-refresh/` (outside the repo). |
| 5 | ownership | **RISKY (carryover)** | MEMORY (oddsportal spike) says `evmax/research/oddsportal.py` + harness "built + tested" | `?? evmax/research/` still untracked, plus `scripts/validate_oddsportal.py`, `tests/test_oddsportal_research.py`, `scripts/model_vs_close.py`, `tests/test_model_vs_close.py` | Work product uncommitted / at risk of loss; repo docs don't cite it so no doc fix applies | **Flagged again** — commit or archive from the main checkout if still wanted. |

## Clean areas verified (no drift)

Every concrete model/weight/constant/reference claim I could check against a code symbol agreed:

- `SECTOR_WEIGHT_OVERRIDES` (soccer, worldcup, nba, wnba, tennis, baseball, nfl, nhl) — all weights match CLAUDE.md verbatim (tennis surface 0.30 / serve_return 0.10 / form 0.35 / advanced 0.15 / h2h 0.05 / ranking_trend 0.05; baseball pitcher 0.50 / elo 0.25 / form 0.25; nfl 0.25/0.25/0.20/0.30/0.0; wnba 0.40/0.45/0.15/0.0/0.0; nba, soccer, worldcup, nhl all match).
- `poisson_agent.py`: `SUPPORTED_SECTORS = SOCCER_LIKE_SECTORS = {"soccer", "worldcup"}` ✓.
- `REQUIRED_BLEND_MODELS["tennis"] = {surface, serve_return, form, advanced}` ✓; `SPREAD_SIM_WEIGHT_OVERRIDES["wnba"] = 0.0` ✓.
- `derive_advance_prob` (PR #74): `ADVANCE_ET_SHARE = 0.55`, `P(adv) = p_win + p_draw·(0.55·r + 0.45·0.5)` — matches CLAUDE.md + `categories.yaml` exactly.
- `SECTOR_SERIES_MAP` ↔ `categories.yaml` consistent (`validate` ✓); worldcup `KXWCADVANCE` + `baseball_props` series present and match the notes.
- All `scripts/*.py` and `data/models/*.json` paths named in CLAUDE.md resolve on disk.
- All CLI command groups (`agents`, `cleanup`, `update`, `categories`, `sim`, `report`, `backtest`, `archive`, `project`, `dashboard`, `portfolio`) and cleanup subcommands named in the Daily Workflow (`close-lines`, `watch-closes`, `watch-listings`, `listings-eval`) exist.
- launchd agents match `SCHEDULED_RUNS.md`: `com.evmax.watch-closes` + `com.evmax.watch-listings` loaded; `com.evmax.nba-resolve` disabled (`.plist.disabled`).

## SAFE fixes applied

- `2f8dbd4` — catalog count 14 → 15 + `baseball_props` (CLAUDE.md:7, README.md catalog table)
- `af63a76` — SCHEDULED_RUNS.md: document the `ev-scan-90min-*` pair, retire the two fixed scans to the Disabled table, 2026-07-02 History note

Both commits are doc-only (no `evmax/**`, `data/categories.yaml`, model-state, test, or fixture files touched), so no test-gate/change-validator pass was required.

## RISKY — needs your decision

- **WIP test failures** (finding 3): 5 failing tests in the untracked `tests/test_wnba_efficiency_carryover.py`. Finish or discard the WNBA carryover experiment; not part of committed main.
- **Delete the disabled `weekly-tennis-rankings-refresh` task folder** (finding 4) — outside the repo, CLI can't do it here.
- **Commit or discard the uncommitted `evmax/research/` + oddsportal/model_vs_close WIP** (finding 5) — still at risk of loss.

**Bottom line: 5 findings — 2 auto-fixed (doc-only), 3 proposed/noted; all model/weight/constant/reference/registry claims otherwise clean.**
