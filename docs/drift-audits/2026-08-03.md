# Drift Audit — 2026-08-03

| Field | Value |
|---|---|
| Date | 2026-08-03 (Mon, weekly scheduled run) |
| Scope | `all` |
| Audited SHA | `abd5a5840e4726cb013392b6f82dc306a6eb9e69` (main) |
| Branch | `drift-audit/2026-08-03` |
| Spec followed | `.claude/commands/drift-audit.md` |

## Deterministic checks

| Check | Result |
|---|---|
| `evmax categories validate` | ✅ Registry valid — 17 categories |
| `pytest tests/ -q` (baseline, pre-change) | ⚠️ **5 failed, 2716 passed** |
| `pytest tests/ -q` (post-change) | ⚠️ 5 failed, 2716 passed — byte-identical, no regression |
| `check_doc_sync.py` | Advisory only (sector-handler zero-coverage nudges); used to aim the sweep |
| Doc reference scan (`scripts/`, `evmax/`, `data/` paths in CLAUDE.md/README/TODO/docs) | ✅ **Zero dead references** |
| Documented `evmax <group> <cmd>` invocations | ✅ All resolve in `evmax/cli/commands/` |

**On the 5 failures — not committed-tree drift.** All five are in
`tests/test_wnba_efficiency_carryover.py`, which is an **untracked** file (`git ls-files` has no
match). It imports `seed_wnba_efficiency.TeamSeasonTotals` / `regress_totals` / `merge_totals`,
none of which exist in the tracked `scripts/seed_wnba_efficiency.py`. This is another session's
uncommitted WIP (alongside untracked `evmax/research/`, `scripts/experiment_wnba_ml_carryover.py`).
Per the guardrails, nothing was edited to make it pass. It is listed as RISKY-7 below so the
owner sees it.

## Findings

15 SAFE (applied) · 7 RISKY (proposed only). Every row cites file:line on both sides.

### SAFE — applied on this branch

| # | Taxonomy | Doc location | Code location | Disagreement | Action |
|---|---|---|---|---|---|
| S1 | Functionality | `CLAUDE.md:19` | `data/categories.yaml` (7 `live`, 10 `shadow`) | Doc: "`live` … Default for every category in the shipped YAML **except `nfl_props`**". Actually 10 categories ship `shadow`. Materially misleading — implies everything is live | Replaced with the explicit live list + a pointer to `evmax categories modes` as the runtime truth |
| S2 | Functionality | `CLAUDE.md:151` | `wnba_efficiency_agent.py:62,69` | Modeling table said `MIN_GAMES=12`; code is `MIN_GAMES = 4` (`SHRINK_K=8` absorbs the noise). CLAUDE.md's own WNBA section already said 4 — the doc contradicted itself | Corrected to 4 with the shrinkage rationale |
| S3 | Functionality | `CLAUDE.md:170` | `elo_agent.py:67,133` | "K-factor / home-advantage **never calibrated for NHL**, NHL half of MODEL-2 still open". Code has `K_FACTORS["nhl"]=6.0` / `HOME_ADVANTAGE_ELO["nhl"]=48.0` with a dated MODEL-2 2026-07-18 sweep comment; `scripts/backtest_nhl_elo.py` exists; `elo_state.json['nhl']` is seeded (34 teams) | Rewritten: calibration CLOSED 2026-07-18; the `elo: 0.0` weight is now an **unmade blend decision, not a blocked one** |
| S4 | Functionality | `CLAUDE.md:181` | same | "generic Elo held at 0 **pending NHL calibration**" — the precondition is met | Rewritten as an open blend decision needing a walk-forward |
| S5 | Functionality | `CLAUDE.md:186` | same | "NHL half of MODEL-2 still open" | Corrected to closed 2026-07-18 (K=6 / home 48) |
| S6 | Ownership | `TODO.md:152-156` | same | MODEL-2 open with a *guessed* target "Add NHL: K=16, home_adv=0.04" — shipped values are K=6 / home_adv=48 | NHL sub-item marked shipped with the real numbers + sweep protocol |
| S7 | Ownership | `TODO.md:721` | same | SECTOR-1: "NHL … Elo uses NBA defaults" | Marked shipped 2026-07-18; notes the remaining item is the ensemble weight |
| S8 | Ownership | `TODO.md:743` | same | Priority table: "Uncalibrated K + home adv … NHL is the last holdout" | Rescoped (see RISKY-1 — `ncaaf` is now the holdout) |
| S9 | Functionality | `CLAUDE.md:210` | `ev_gap_agent.py:178-190` | Bullet titled "Sharp-only guard **(soccer/worldcup)**" and lists unaffected sectors without `ncaaf`, but `MIN_NONSHARP_MODELS` has an `ncaaf` moneyline entry | Retitled soccer/worldcup/**ncaaf**; added the ncaaf rationale |
| S10 | Reference | `README.md:1575` + table | `data/categories.yaml` (17 keys) | "**16 categories** ship today" and the table omits `ncaaf` entirely (added 2026-08-01) | Count → 17; added the `ncaaf` row (ML only, spread/total disabled, shadow/wip) |
| S11 | Functionality | `README.md:1587`, `README.md:1608` | `data/categories.yaml:142` | Says wnba `spread`+`total` "→ shadow". They are `disabled_market_types` (ownership moved to the anchored-entry trigger 2026-07-19) | Corrected both; noted no category populates `shadow_market_types` today |
| S12 | Functionality | `README.md:1594` | `data/categories.yaml:223` | `baseball_props` resolver listed as `espn_boxscore`; registry says `mlb_statsapi` (CLAUDE.md is explicit: "NOT ESPN") | Corrected to `mlb_statsapi` |
| S13 | Reference | `README.md:1615` + series table | `kalshi.py:48,57` | NFL row missing `KXNFLSPREAD`; no NCAAF row at all | Added `KXNFLSPREAD` and the `KXNCAAFGAME/SPREAD/TOTAL` row |
| S14 | Functionality | `injury_agent.py:24` (docstring) | `injury_agent.py:87,234,561` | Docstring: "capped at **-0.12** per team". Real caps: `MAX_ADJ = 0.20` report-level, then a one-sided **-0.10** floor in `apply_adjustments`. Also omitted NCAAW/WNBA from data sources and 3 of 6 `STATUS_IMPACT` statuses | Docstring corrected on all three points (comment-only) |
| S15 | Reference | `cleanup/__init__.py:5` | `logger.py` (0 `ev_outcomes` refs), `resolver.py:1115` | Claimed "logger.py — writes EVGaps to ev_predictions **+ ev_outcomes**", and "resolver … updates `ev_outcomes.result`" (no such column) | Corrected: logger writes `ev_predictions` + `prop_observations`; resolver is the sole `ev_outcomes` writer |

**Scheduled-run ownership drift (all in `docs/SCHEDULED_RUNS.md`, applied):**

| # | Taxonomy | Disagreement | Action |
|---|---|---|---|
| S16 | Ownership | `weekly-value-audit` listed as an active Weekly task **and** named in the PR-within-the-run policy. The task is `enabled: false` since 2026-08-03, superseded by `biweekly-model-improve-graph` | Moved to Disabled with the supersession reason; PR policy now names `weekly-drift-audit` + `biweekly-model-improve-graph` |
| S17 | Ownership | **Three enabled tasks were entirely undocumented**: `weekly-calibration-tripwire` (Mon 08:34), `biweekly-model-improve-graph` (Mon+Thu 08:51), `weekly-wnba-total-anchored-backfill-check` (Mon 09:01) | All three added to the Weekly table; table re-sorted by clock time |
| S18 | Reference | `weekly-tennis-rankings-refresh` documented as "Still present as a disabled task — safe to delete the task folder". It is deregistered **and** its `~/.claude/scheduled-tasks/` folder is gone | Row corrected to DELETED (confirmed 2026-08-03) |
| S19 | Ownership | `weekly-seasonal-model-reseed` "What it does" omitted the NCAAF EPA reseed, which the task has covered since 2026-08-01 (its own description and CLAUDE.md both include it) | Added "NCAAF efficiency EPA (Aug–Jan)" |
| S20 | Ownership | `daily-close-lines-capture` (ENABLED, daily 08:04) documented as "snapshots Pinnacle closing lines pre-tipoff". **It cannot capture anything** — see below | Added a ⚠ block documenting the structural no-op; task left enabled and untouched pending a human decision (RISKY-6) |

**S20 evidence.** `cleanup close-lines` (`cleanup.py:396-405`) selects
`FROM ev_outcomes WHERE outcome IS NULL AND pinnacle_close_prob IS NULL`. The only writer of
`ev_outcomes` is `resolver._write_outcome` (`resolver.py:1115`), which always inserts a concrete
`outcome`; every other reference is an `UPDATE … pinnacle_close_prob`. Measured on
`data/predictions.db`: **3741 rows, 0 with `outcome IS NULL`, 0 matching the query.** The task
prints "No unresolved markets without closing lines" every single day. This confirms the
`close-lines-command-dead` memory note still holds, and the doc had never been annotated.

### RISKY — needs your decision (nothing applied)

| # | Taxonomy | Doc side | Code side | Disagreement & recommendation |
|---|---|---|---|---|
| R1 | Functionality | `TODO.md` MODEL-2 (now scoped), `data/categories.yaml:96` | `elo_agent.py` (**zero** `ncaaf` occurrences), `elo_agent.py:640-641`, `ensemble_agent.py:181-186` | **`ncaaf` has no `K_FACTORS` / `HOME_ADVANTAGE_ELO` entry**, so it silently takes the fallbacks: `K_FACTORS.get(sector, 20.0)` = 20.0 (identical to NBA's) and `HOME_ADVANTAGE_ELO.get(sector, 0.0)` = **0.0 home-field edge for college football** — while `ncaaf` carries generic elo at weight **0.25**. A zero CFB home bonus is near-certainly wrong. This is the "CFB-calibrated generic Elo" that categories.yaml lists as PHASE 2 work. **Recommend:** sweep ncaaf K + home_adv (the NCAAW/NHL protocol) before ncaaf promotion. Caught by the change-validator gate after I over-generalized MODEL-2 as closed — the TODO entry now records it as OPEN. |
| R2 | Ownership | `evmax/cli/commands/update.py:31` help text advertises ncaaw | `cleanup.py:256` `_UPDATE_HOOK_SECTORS = ["soccer","worldcup","nba","wnba","nfl","ncaab","nhl","baseball"]` | The resolve-time model-update hook (and the `evmax update scores` default `--sectors`) **omit both `ncaaw` and `ncaaf`**. So the NCAAW Elo calibrated on 2026-07-11 (K=35/home 80) is never incrementally fed — `elo_state.json['ncaaw']` has `last_updated=None` — and `ncaaf` is absent outright. **Recommend:** add `ncaaw` + `ncaaf` to `_UPDATE_HOOK_SECTORS` and the CLI default. Code change → not applied here. |
| R3 | Functionality | `data/categories.yaml:154` (`nhl` notes) | `elo_agent.py:67,133` | Notes still say "Elo K-factor **still uncalibrated** for NHL (see MODEL-2 / SECTOR-1)". Contradicted by S3. Left unedited because the spec classes **any** `categories.yaml` edit as RISKY. **Recommend:** update the note (doc-only within the YAML). |
| R4 | Functionality | `data/categories.yaml:135` (`baseball` notes) | `data/categories.yaml:129` | Notes claim "`shadow_market_types: [total]` **is declared** ahead-of-time", but the entry has no `shadow_market_types` field — only `disabled_market_types: [total]` (totals were disabled 2026-06-10). No category in the shipped YAML populates `shadow_market_types` at all. **Recommend:** correct the note. |
| R5 | Reference | `data/categories.yaml:215` (`nfl_props` notes) | `kalshi.py:72-79` | Notes claim "kalshi.py `SECTOR_SERIES_MAP` **still has phantom series names** (KXNFLPAS etc.); the fix ships with MODEL-9". Already fixed — the map carries the real tickers (`KXNFLPASSYDS`, `KXNFLRSHYDS`, …) and an in-code comment saying the short names "are not real tickers". **Recommend:** correct the note. |
| R6 | Ownership | `docs/SCHEDULED_RUNS.md` (now annotated) | `cleanup.py:396-405`, `resolver.py:1115` | The dead `close-lines` query behind an enabled daily task (S20). **Recommend:** either repoint the query at unresolved `ev_predictions` rows (a real fix, and the pre-tipoff Pinnacle snapshot the task was meant to take has genuine value), or retire `daily-close-lines-capture` since `com.evmax.watch-closes` + `cleanup backfill-clv` already cover close capture. Code/task change → not applied. |
| R7 | — | — | `tests/test_wnba_efficiency_carryover.py` (untracked) | 5 failing tests from another session's uncommitted WIP; the tracked `scripts/seed_wnba_efficiency.py` lacks the `TeamSeasonTotals`/`regress_totals`/`merge_totals` symbols the tests import. Not committed-tree drift and **deliberately untouched** (guardrail: never edit a test to make a check pass). **Recommend:** the WNBA carryover work either lands with its script changes or the test file is removed. |

## Clean areas (verified, no drift)

Checked symbol-by-symbol and found **fully consistent** with the docs:

- **All 12 `SECTOR_WEIGHT_OVERRIDES` entries** ↔ the CLAUDE.md per-sector override bullets (soccer, worldcup, nba, wnba, tennis, baseball, nfl, ncaaf, ufc, nhl, ncaab, ncaaw) — every weight matches to the digit.
- **Poisson gating** — `SUPPORTED_SECTORS` = `SOCCER_LIKE_SECTORS` = `{soccer, worldcup}`; worldcup symmetric league avg 1.30/1.30.
- **Blend gates** — `REQUIRED_BLEND_MODELS["tennis"]` (the 4 primaries) and the `_NON_MODEL_TOKENS` set match CLAUDE.md verbatim.
- **Model constants** — NBA 3.2/12.0 · WNBA 2.6/12.5/SHRINK_K 8 · pace clips [65,100] and [80,120] · NCAAB & NCAAW 10.5/6.0/4 · NCAAF 2.5/16.5/70/RAMP_K 3 · NFL efficiency 2.0/13.5/64/6 · NHL 0.18/2.05 · NFL QB Elo K=25/home 48/share 0.60/clamp ±200 · Elo & Form `STALE_DAYS=60` · worldcup Elo K=40/home 0 · NCAAW Elo K=35/home 80.
- **Tennis sub-agents** — h2h `_MIN_MEETINGS=3` / `_MAX_NUDGE=0.18`; ranking trend `_RANK_ALPHA=0.40` / `_MOMENTUM_K=0.15` / `_MAX_LOGIT=0.40` / `_TREND_WEEKS=12` / `_STALE_WEEKS=6`; serve/return `_K_BO3=14.0` / `_K_BO5=18.0`; synthetic `games_per_player=12`.
- **Infra constants** — `fuzzy_threshold=88`; Kalshi `AsyncLimiter(10,1.0)`; PolyUS `AsyncLimiter(15,1.0)`; Kalshi taker 0.07 / PolyUS taker 0.06 / maker −0.0125; Kelly max 0.05, exposure 0.08, `joint_kelly_max_gross_pct` 0.15.
- **Registry plumbing** — `KNOWN_RESOLVERS` matches the documented resolver set exactly; `POLYMARKET_US_LEAGUE_MAP` is 8 sectors as documented; `_US_SECTORS` includes both `ncaaf` and `ufc`; off-season shadow downgrade confirmed live (`ncaab`/`ncaaw`/`nfl` show `live` base → `shadow` effective).
- **Reference integrity** — every `scripts/*.py`, `evmax/**`, `data/models/*.json` path and every `evmax <group> <cmd>` named across CLAUDE.md / README.md / TODO.md / `docs/` resolves on disk.

## Bottom line

**22 findings — 15 auto-fixed, 7 proposed, 8 clean areas.**

The dominant theme is **stale "still open" claims**: NHL Elo calibration closed 2026-07-18 but four
docs still called it open, and the scheduled-task inventory had drifted three enabled tasks behind
reality (including the 2026-08-03 value-audit → model-improve-graph supersession). The two most
consequential findings are code-side and are therefore proposals, not fixes: `ncaaf` runs generic
Elo with a **0.0 home-field advantage** (R1), and the resolve-time model-update hook never feeds
`ncaaw` or `ncaaf` (R2). One enabled daily task is a confirmed structural no-op (R6).
