# Drift audit — 2026-07-01 (loop 1 of 3)

- **Scope:** live sectors (user-requested run, 3 loops)
- **Audited SHA:** `d68cd36` (origin/main, post PR #66)
- **Deterministic checks:** `evmax categories validate` ✓ (15 categories) · `pytest tests/ -q` → **1944 passed, 7 skipped** · `check_doc_sync.py` advisory only (zero-coverage nudges, no drift)

## Findings

| # | Taxonomy | SAFE/RISKY | Doc side | Code/reality side | Disagreement | Action |
|---|---|---|---|---|---|---|
| 1 | reference | SAFE | CLAUDE.md Architecture tree | deleted in `0f27511` (2026-06-19) | `clients/pinnacle.py`, `models_ml/live_win_prob.py`, `pipeline/live_scanner.py`, `cli/commands/opportunities.py` (listed twice) + `evmax opps` still in the tree | Fixed — cherry-picked the stranded local commit `ba84540` from the never-pushed `drift-audit/2026-06-29` branch |
| 2 | reference | SAFE | CLAUDE.md Architecture tree | `evmax/cli/commands/`, `evmax/sectors/`, `evmax/` top level | Tree omitted 6 real CLI modules (`shadow.py`, `categories.py`, `report.py`, `project.py`, `portfolio.py`, `dashboard.py`), duplicated the `sectors/` entry, and the sector file list lacked wnba/worldcup/baseball/nhl; top-level `categories.py`/`modes.py`/`portfolios.py`/`players/`/`web/` unlisted | Fixed — tree now matches the module list |
| 3 | functionality | SAFE | CLAUDE.md:344 (Scan Pipeline mermaid) | `injury_agent.py:557` `_adj_cap = 0.10`, `:83` `MAX_ADJ = 0.20` | Diagram said "−12% per team cap"; code caps −10% per team (−20% report-level), and the Key Pipeline prose already said 10/20. 12% appears nowhere in code | Fixed — diagram now says −10% |
| 4 | ownership | SAFE | CLAUDE.md:237 Daily Workflow | crontab removed 2026-06-28; `launchctl` shows `com.evmax.watch-closes` + `com.evmax.watch-listings` loaded | "(also wired as a */5 cron)" — no crontab exists; watch-closes is owned by launchd | Fixed — both watcher blocks now name their launchd agents |
| 5 | ownership | SAFE | docs/SCHEDULED_RUNS.md (whole file) | `~/.claude/scheduled-tasks/` (10 enabled + 2 disabled) + 2 launchd agents | Doc described superseded claude.ai cloud routines, a May-2026 calendar, and tennis-shadow validation (tennis is `live` in categories.yaml) | Fixed — rewritten to the verified current inventory, old cloud-routine content moved to a History note |
| 6 | ownership | RISKY (note) | CLAUDE.md tennis section says the `weekly-tennis-rankings-refresh` task was "deleted" | task exists with `enabled: false` | Disabled ≠ deleted; the task folder still exists with a stale Sackmann-era prompt | Not auto-applied (outside the repo). Recommend deleting `~/.claude/scheduled-tasks/weekly-tennis-rankings-refresh/` |
| 7 | ownership | RISKY (note) | MEMORY (oddsportal spike) says `evmax/research/oddsportal.py` + validation harness "built + tested, 13 tests" | not on origin/main; exists only **uncommitted** in the main checkout (`/Users/ktzeng/Projects/evmax/evmax/research/`) | Work product at risk of loss; repo docs don't cite it so no doc fix applies | Flagged — commit it (or archive it) from the main checkout if still wanted |

## False positives dropped (verified against code before dismissal)

- Tennis ranking_trend formula "disagreement": code is exactly `logit = 0.40·Δlog(rank) + clamp(0.15·momentum, ±0.40)` (`tennis_ranking_trend_agent.py:44-48`) — matches CLAUDE.md verbatim.
- `scripts/validate_oddsportal.py` "referenced in CLAUDE.md but missing": CLAUDE.md does not reference it (grep-verified); it lives uncommitted in the main checkout (see finding 7).

## SAFE fixes applied

- `ba84540` (cherry-picked): drop removed Phase-1 modules from the architecture tree
- CLAUDE.md: tree modernization + injury-cap mermaid fix + launchd ownership notes
- docs/SCHEDULED_RUNS.md: full rewrite to current scheduling reality

## RISKY — needs your decision

- Delete the disabled `weekly-tennis-rankings-refresh` scheduled-task folder (finding 6).
- Commit or discard the uncommitted `evmax/research/` module in the main checkout (finding 7).
- If any claude.ai cloud routine is still enabled, disable it at claude.ai/code/routines (unverifiable from CLI).

**Bottom line: 7 findings — 5 auto-fixed (doc-only), 2 proposed, model/weight/constant claims otherwise clean (83/85 checks agree).**

---

# Loop 2 — Betting Categories prose, props model reality, TODO staleness

Deterministic checks re-run after fixes: `pytest` → 1944 passed / 7 skipped.

| # | Taxonomy | SAFE/RISKY | Doc side | Code/reality side | Disagreement | Action |
|---|---|---|---|---|---|---|
| 8 | functionality | SAFE | CLAUDE.md:7 catalog | `data/categories.yaml` (15 entries, `validate` confirms) | "14 categories" and the prop list omitted `baseball_props` (added 2026-06-27) | Fixed — 15, `baseball_props` listed |
| 9 | functionality | SAFE | CLAUDE.md mode table (`live` row) | YAML: 7 live / 8 shadow | "Default for every category in the shipped YAML except `nfl_props`" — false since the May–June demotions | Fixed — points at the YAML instead of enumerating |
| 10 | functionality | SAFE | CLAUDE.md Key Pipeline step 3 + FETCH mermaid | `injury_agent.py:50-61` ESPN paths | WNBA injuries are fetched but both doc spots omitted WNBA | Fixed — WNBA added |
| 11 | functionality | SAFE | CLAUDE.md Modeling table (NBA/NFL Props Cache rows) | `edb3d7b` (2026-05-10) ripped out the L15 prob model; `_evaluate_prop_pair` is live via anchor pricing (`ev_gap_agent.py:1269` docstring), `nba_props_cache.py`/`nfl_props_cache.py` are diagnostics-only | Rows still described "per-36 normalization + opponent adj" L15 models | Fixed — rows now describe anchor pricing + diagnostics-only caches |
| 12 | reference | SAFE | CLAUDE.md:52 resolver list, :455 prop-category list | YAML | `espn_boxscore` and the `::prop::` prose omitted `baseball_props`/MLB | Fixed |
| 13 | ownership | SAFE | README.md:222 | launchd `com.evmax.watch-closes` | "(or the bundled `*/5` cron)" — crontab removed 06-28 | Fixed |
| 14 | reference | SAFE | TODO.md ARCH-1 [P2] | `0f27511` deleted all three files | Item still open though fully done 2026-06-19 | Fixed — struck through as DONE |
| 15 | reference | SAFE | TODO.md ARCH-3 / ARCH-9 / MODEL-7 | `0f27511`, `edb3d7b` | ARCH-3's pinnacle.py bullet done; ARCH-9's "never deleted" premise false (file deleted — needs git-history recovery); MODEL-7 lists deleted `evmax/backtest/sources/nfl_props.py` | Fixed — premises corrected, deletions annotated |
| 16 | functionality | SAFE | `evmax/clients/nfl_props_cache.py` module docstring | functions in file (no `compute_nfl_prop_prob*` remains) | Docstring described the deleted 8-stage L15 prob model | Fixed — docstring-only edit; full suite re-run green |
| 17 | functionality | RISKY | `data/categories.yaml` `nba_props.notes` | `edb3d7b` | Notes still say "per-36-min normalization + opponent adjustment + streak/margin blends" — that model no longer exists | NOT applied (categories.yaml edits are RISKY by rule). Proposed: update notes to describe anchor pricing |
| 18 | ownership | RISKY (note) | memory `project_props_validation_gate.md` | `_evaluate_prop_pair` is enabled (shadow-gated categorically) | Memory says the evaluator "stays disabled" — stale since 05-10 | Memory updated separately (not a repo change) |

**Loop 2 bottom line: 11 findings — 9 auto-fixed, 2 proposed (categories.yaml notes + memory).**
