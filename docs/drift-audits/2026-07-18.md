# Drift Audit — 2026-07-18

- **Scope:** all
- **SHA audited:** `096d2e44736caa1d5695ae96665bca2505e1ae76` (main, pre-audit)
- **Branch:** `drift-audit/2026-07-18`

## Deterministic checks

| Check | Result |
|---|---|
| `evmax categories validate` | ✓ Registry valid, 16 categories |
| `scripts/check_doc_sync.py` | Only advisory "file changed, check doc" hints (fired broadly, e.g. any `evmax/agents/models/*.py` touch flags the whole Modeling table) — used to aim the semantic sweep, not treated as findings by itself |
| `pytest tests/ -q` | 2377 passed, 7 skipped, 0 failed |

## Findings

| # | Taxonomy | Doc location | Code location | Disagreement | Action |
|---|---|---|---|---|---|
| 1 | Functionality (doc gap) | `CLAUDE.md` Modeling table + Per-sector overrides (no NHL entries) | `evmax/agents/models/nhl_xg_agent.py`, `ensemble_agent.py:177-182` (`SECTOR_WEIGHT_OVERRIDES["nhl"]`), `data/categories.yaml:136-143` | CLAUDE.md's Modeling table and per-sector override bullets cover every sector except NHL, even though `nhl_xg_agent.py` has shipped since 2026-05-02 and `categories.yaml`'s `nhl` notes field already documents it in detail. Doc was simply never updated when the model landed. | **SAFE — fixed.** Added an "NHL xG" Modeling table row and an NHL per-sector override bullet, sourced verbatim from the ensemble code + categories.yaml notes. Commit `adabb47`. |
| 2 | Ownership | `docs/SCHEDULED_RUNS.md` "Daily" table — `ev-scan-90min-on-hour` / `ev-scan-90min-half-hour` described as the live rolling ~90-min scan+push-notify cadence that replaced the old fixed 1am/9am scans on 2026-07-02 | `~/.claude/scheduled-tasks/ev-scan-90min-on-hour/SKILL.md` and `.../ev-scan-90min-half-hour/SKILL.md` exist on disk, **but `list_scheduled_tasks` does not return either task** — every other documented task (including the disabled `daily-morning-scan`/`daily-updated-scan` these two supposedly replaced) shows up in the live registry; these two do not, not even as `enabled: false` | Confirmed by user (2026-07-18): the two tasks were **intentionally removed** — SKILL.md folders left on disk but deregistered from the scheduler. No longer ambiguous once confirmed, so this flips from RISKY to a **SAFE doc fix**. | **SAFE — fixed** (once confirmed). Updated `docs/SCHEDULED_RUNS.md`: removed the `ev-scan-90min-*` row from the live Daily table, added an explicit "no automated `evmax agents scan` currently runs" note, moved the pair into the Disabled/removed table, and logged a 2026-07-18 History entry. Commit `0becec3`. |

## Clean areas (checked, no drift found)

- `poisson_agent.py::SUPPORTED_SECTORS`/`SOCCER_LIKE_SECTORS` == `{"soccer", "worldcup"}` — matches CLAUDE.md.
- `ensemble_agent.py::SECTOR_WEIGHT_OVERRIDES` for nba/wnba/soccer/worldcup/tennis/baseball/ufc/nfl/ncaab/ncaaw — all weights match CLAUDE.md verbatim (NHL was the only gap, see Finding 1).
- `time_util.py::_US_SECTORS` — matches the UFC/`_US_SECTORS` claim in CLAUDE.md's UFC section.
- All `evmax <command> <subcommand>` invocations named in CLAUDE.md's Daily Workflow resolve against the live CLI (`evmax --help` tree walked for agents/cleanup/cleanup shadow/archive/categories/project/sim/update/arb/backtest).
- All `scripts/*.py` and `docs/*.md` paths referenced in CLAUDE.md exist on disk.
- All `data/models/*.json` state files referenced in CLAUDE.md exist on disk.
- Every other task in `docs/SCHEDULED_RUNS.md` matches its live `list_scheduled_tasks` entry (cadence, enabled state) — `weekly-tennis-rankings-refresh` and `weekly-nba-props-shadow-metrics` correctly documented as disabled/deprecated.

## SAFE fixes applied

- `adabb47` — docs(claude): document NHL xG model and NHL ensemble weights
- `0becec3` — docs: reflect intentional removal of ev-scan-90min-* tasks (user-confirmed 2026-07-18)

## RISKY — needs your decision

None outstanding. Finding 2 was reported RISKY pending confirmation of which side (doc vs. registry) was correct; the user confirmed the removal was intentional, so it was fixed as a SAFE doc update in this same run rather than left open.

## Bottom line

2 findings — 2 auto-fixed (1 direct, 1 after user confirmation), 0 proposed, 6 clean areas checked.
