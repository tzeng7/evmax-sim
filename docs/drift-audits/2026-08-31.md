# Drift Audit — 2026-08-31

| | |
|---|---|
| **Scope** | `all` |
| **Audited SHA** | `7998bf9` (`origin/main` tip — "chore(models): weekly seed state 2026-08-30") |
| **Branch** | `drift-audit/2026-08-31` |
| **Previous audit** | 2026-08-03 (4 weeks — the largest gap so far; drift accumulated accordingly) |

**Run isolation note.** This audit ran in an isolated git worktree created by
`scripts/sched_worktree.py open --branch drift-audit/2026-08-31`, **not** by `git switch -c` in the
shared checkout, which is what `.claude/commands/drift-audit.md` still instructs. That was a
deliberate deviation: the shared checkout held 15 uncommitted `data/models/*.json` files belonging
to other scheduled tasks at run start, and a full `pytest` run there is known to mutate exactly
those files. See finding **R1** — this is itself the highest-priority drift found.

## Deterministic checks

| Check | Result |
|---|---|
| `evmax categories validate` | ✅ Registry valid. 17 categories. |
| `scripts/check_kalshi_series.py` | ✅ `OK: 61 · STALE: 0 · NEW: 268` — every wired ticker still exists on Kalshi. |
| `pytest tests/ -q` | ✅ **3201 passed**, 43 warnings (re-run green after edits). |
| `check_doc_sync.py` | advisory only — used to aim the semantic sweep, no findings raised on its own. |

The 268 `[NEW]` Kalshi series are informational (prefix matches such as `KXLIGUE1BTTS`, `KXLIGUE1CORNERS`,
`KXCS2MAP`, `KXCSGO*` — exotic markets we deliberately do not wire). No `[STALE]` tickers.

## Findings

16 findings — 14 SAFE (auto-fixed), 2 RISKY (proposed).

### SAFE — applied on this branch

| # | Taxonomy | Doc location | Code location | Disagreement | Action |
|---|---|---|---|---|---|
| S1 | functionality | `CLAUDE.md:19` | `data/categories.yaml` (worldcup/nba_props/nfl_props/baseball_props `mode: disabled`; set in `3a3a30e` "chore(categories): disable worldcup + player-prop sectors (#185)", 2026-08-08) | Mode table claimed "every other category ships `shadow`" — four categories ship `disabled`. | Rewrote the cell to name the four disabled categories. |
| S2 | functionality | `CLAUDE.md:13` | `data/categories.yaml:111` | worldcup section header said "(national-team World Cup, **shadow**)". | → `disabled`. |
| S3 | functionality | `CLAUDE.md:20` | `data/categories.yaml:212` | Shadow-mode row said "Used by MODEL-9 validation for NFL props" — `nfl_props` is `disabled`, so nothing accrues. | Repointed to the sectors actually in shadow today (ncaaf/ufc/nhl/baseball/lol/cs2). |
| S4 | functionality | `CLAUDE.md:33` (PolyUS paragraph) | `data/categories.yaml:142` `disabled_market_types: [spread, total]` | Claimed "wnba spread/total are `shadow_market_types`". **No** category populates `shadow_market_types` at all, and CLAUDE.md's own WNBA section says `disabled_market_types` — a self-contradiction. | → `disabled_market_types`. |
| S5 | functionality | `CLAUDE.md:28` | `data/categories.yaml:93` `resolver: espn_scoreboard` | The `espn_scoreboard` resolver list omitted `ncaaf`. | Added NCAAF + its `groups=80` FBS filter. |
| S6 | ownership | `CLAUDE.md:309` | `~/Library/LaunchAgents/com.evmax.watch-listings.plist` | Said "add `--log-entries` to its plist post-merge" — already done. | Documented the actual args (`--once --log-entries --entry-sectors wnba,baseball`). |
| S7 | functionality | `CLAUDE.md` worldcup section | `data/categories.yaml:111` | "Promote with `evmax cleanup shadow promote worldcup`" presumes shadow; a `disabled` category logs nothing to promote on. | Noted that it must be re-enabled to `shadow` first. |
| S8 | functionality | `README.md:1602,1610-1612` | `data/categories.yaml` | Category snapshot table listed worldcup / nba_props / nfl_props / baseball_props as `shadow`. | → `disabled`. |
| S9 | functionality | `README.md:7`, `:1566`, `:1659` | `evmax/settings.py:48,60,161`; enforced at `coordinator.py:266`, `logger.py:179`, `web/app.py:499` | Described the Polymarket US firewall as venue-wide ("until `polymarket_us_live=true`, **every** PolyUS gap logs as shadow"). It has been **per-sector** since 2026-08-22, with `polymarket_us_live_sectors="wnba"` shipped. | Rewrote all three sites; added the missing `POLYMARKET_US_LIVE_SECTORS` env-table row. |
| S10 | reference | `README.md:1540-1541` | `evmax/cli/app.py:49` + `evmax/cli/commands/report.py:26` | `evmax report` and `evmax report bankroll` are both invalid — `report` is a Typer sub-app whose only command is `report`, with `--bankroll` as a flag. Verified empirically (exit 2, "No such command 'bankroll'"). | → `evmax report report` / `evmax report report --bankroll`. |
| S11 | ownership | `docs/SCHEDULED_RUNS.md:149` (launchd table) | `~/Library/LaunchAgents/com.evmax.{heartbeat,clv-monitor}.plist`, both loaded per `launchctl list` | Two active launchd agents shipped with the reliability-hardening work (PR #231) were entirely undocumented. | Added rows for `com.evmax.heartbeat` (09:00 + 21:00, `cleanup heartbeat --check-pinnacle --notify`) and `com.evmax.clv-monitor` (Mon 09:20, `cleanup clv-monitor --notify`). |
| S12 | ownership | `docs/SCHEDULED_RUNS.md:148` | same plist as S6 | watch-listings row showed bare `--once`. | Corrected to the real args. |
| S13 | ownership | `docs/SEASON_START.md:25` | `~/.claude/scheduled-tasks/weekly-seasonal-model-reseed/SKILL.md` (5 seed scripts) | Said the reseed task "Covers exactly three sports" and named "MLB pitcher FIP". It runs five: WNBA, NFL (efficiency + QB Elo), **NCAAF EPA**, MLB **pitcher_v2**, **UFC Glicko-2**. | Corrected the row. |
| S14 | functionality | `docs/SEASON_START.md:244` (open gap list), `:124-131` (open checklist), `TODO.md:465` + `:702` | `elo_agent.py:53,139` (NCAAW K=35 / home_adv=80, shipped 2026-07-11); `kalshi.py:81-88` + `check_kalshi_series.py STALE: 0` (real NFL prop tickers since `58e6150`, 2026-04-15); `coordinator.py:896` `_PROP_SECTORS = {"nba","nfl","baseball"}`; `KALSHI_PROP_SERIES` absent from the tree | Four separate "still to do" items that shipped months ago, one of them (`ARCH-4`) citing a symbol that no longer exists. | Marked NCAAW calibration and the NFL-prop ticker fix DONE with evidence; closed `ARCH-4` and repointed its `PROPS-1` referrer; noted that `nfl_props` must return to `shadow` before rows can accrue. |

One code file was touched, comment-only:

| # | Taxonomy | Doc location | Code location | Disagreement | Action |
|---|---|---|---|---|---|
| S15 | functionality | `evmax/agents/models/ensemble_agent.py:205-210` (comment) | `elo_agent.py:83` `K_FACTORS["nhl"]=6.0`, `:156` `HOME_ADVANTAGE_ELO["nhl"]=48.0`; `elo_state.json['nhl']` seeded | The NHL blend comment said generic Elo is 0 "because its K-factor / home-advantage have **never been calibrated** for NHL". The NHL half of MODEL-2 closed 2026-07-18. CLAUDE.md already documents this correctly; only the code comment lagged. This matters — the stale rationale reads as a *blocker* on raising NHL Elo weight, when it is actually an unmade blend decision. | Corrected the rationale. **Weights untouched.** |

`pytest` re-run green (3201 passed) and the diff was gated through the `change-validator` agent, which
verified the edit is comment-only by AST-comparing the file before and after (identical) and
independently re-verified every factual claim above against live symbols. **Verdict: ACCEPT.**

### RISKY — needs your decision (not applied)

**R1 — ownership — the drift-audit spec still tells the run to branch in the shared checkout.**

- Doc A: `.claude/commands/drift-audit.md` Phase 3 — `git switch -c drift-audit/<date>`, and the
  scheduled-task prompt ends with `git switch main`.
- Doc B: `docs/SCHEDULED_RUNS.md:60-95` — "The shared checkout now **stays permanently on a clean
  `main`** — all work happens in worktrees — so no task ever runs `git switch` / `git reset --hard`
  / `git checkout -- .` / `git clean` there", and lists `weekly-drift-audit` under "Tasks to convert".
- Code: `scripts/sched_worktree.py` is merged (`41267d6`, PR #233) and working — this run used it.

Both docs are version-controlled and they contradict each other. The helper's precondition
("must be on `main` before any task calls it") is now satisfied, so the conversion is unblocked.
This is not academic: at run start the shared checkout held 15 uncommitted `data/models/*.json`
files from other tasks, and the spec's `git switch -c` would have carried them onto the audit
branch while a full `pytest` mutated them.

*Classified RISKY because editing the spec changes how an automated run behaves.* Recommended:
convert Phase 3 of `.claude/commands/drift-audit.md` to `sched_worktree.py open` / `ship --run-hooks`
(dated branch, no `--rolling`), drop the `git switch main` step, and update the matching
`~/.claude/scheduled-tasks/weekly-drift-audit/SKILL.md` (not a repo file). Same conversion is
pending for `weekly-seasonal-model-reseed`, `weekly-tennis-surface-elo-refresh`,
`daily-resolve-and-model-update`, `daily-evening-resolve`, `weekly-model-calibration` (all
`--rolling`) and `biweekly-model-improve-graph`.

**R2 — functionality — three stale `notes:` blocks in `data/categories.yaml`.**

`categories.yaml` is on the audit's never-auto-edit list, so these are reported rather than fixed.
All three are prose-only (`notes:` free text); none affects behavior.

| Entry | Line | Stale claim | Reality |
|---|---|---|---|
| `nhl` | 157 | "Elo K-factor still uncalibrated for NHL (see MODEL-2 / SECTOR-1) — kept out of NHL ensemble for now" | Calibrated 2026-07-18: `K_FACTORS["nhl"]=6.0`, `HOME_ADVANTAGE_ELO["nhl"]=48.0`. Same drift as S15. |
| `nfl_props` | 215 | "kalshi.py SECTOR_SERIES_MAP still has phantom series names (KXNFLPAS etc.); the fix ships with MODEL-9 so no NFL prop markets are fetched in scan today" | Fixed 2026-04-15 (`58e6150`). Real tickers, `STALE: 0`. Nothing is fetched today because the category is `disabled`, which is a different reason. |
| `worldcup`, `nba_props`, `baseball_props` | 111, 201, 223 | Notes still read "Starts SHADOW…", "Demoted to shadow…", "SHADOW until…" | All three are `mode: disabled` since PR #185. |

Recommended: a small `chore(categories)` PR refreshing those three `notes:` strings, with the
`mode:`/`models:`/`resolver:` fields untouched.

### Informational (no action)

- **`weekly-tennis-rankings-refresh`** — the run brief flagged this as a suspected lingering
  "active" claim. **Verified clean**: the task is absent from `list_scheduled_tasks` entirely, and
  `docs/SCHEDULED_RUNS.md` already records it as deprecated *and deleted*. Doc and reality agree.
- **`baseball-spread-lay-gate-check`** — a one-time task that fired 2026-08-30 and is now disabled.
  Not in `SCHEDULED_RUNS.md`; that doc inventories recurring jobs, so this is not drift.
- **268 `[NEW]` Kalshi series** matching our sector prefixes (Ligue 1 corners/BTTS/halves, CS2 map
  markets, `KXCSGO*`). All exotic markets we deliberately do not wire.
- **`docs/SEASON_START.md`** MLB reseed window reads "Mar–Nov" (matching `baseball.season_window`
  `03-20`→`11-05` in the registry) while `weekly-seasonal-model-reseed`'s SKILL.md says
  "April through October". The registry is authoritative and the doc follows it; the *task config*
  is the narrower one. Left alone — the SKILL.md is not a repo file.
- **Undocumented-but-correct**: the two Kalshi fixes from 2026-08-30 (PR #237 tennis
  `yes_sub_title` + event-title join for the new "{Name} wins" titles; PR #238 `get_markets`
  cursor pagination) are absent from CLAUDE.md's Key Implementation Details. That is
  *incompleteness*, not a contradiction — adding new documentation is outside a drift audit's
  scope. Flagging in case you want them written up.

## Bottom line

**16 findings — 14 auto-fixed, 2 proposed, deterministic checks all clean.**

Clean areas verified with no drift: every `SECTOR_WEIGHT_OVERRIDES` entry (12 sectors) vs the
Modeling table; `REQUIRED_BLEND_MODELS`, `MIN_NONSHARP_MODELS`, `_NON_MODEL_TOKENS`;
`PoissonModelAgent.SUPPORTED_SECTORS` / `SOCCER_LIKE_SECTORS`; every documented model constant
(WNBA/NBA/NCAAB/NCAAW/NCAAF/NFL efficiency + possession-sim + NHL xG + QB Elo, incl. all pace
clips); Elo `K_FACTORS` / `HOME_ADVANTAGE_ELO` / `STALE_DAYS`; fee model; Kelly + exposure +
joint-Kelly caps; injury caps; `ADVANCE_ET_SHARE`; sharp_weight bounds and the 0.85 default;
rate limiters; matching threshold; `_US_SECTORS`; `MarketType` enum; every `evmax/**` and
`scripts/**` path cited in docs; and all 67 distinct `evmax <cmd>` invocations across the docs
(only S10 failed to resolve).
